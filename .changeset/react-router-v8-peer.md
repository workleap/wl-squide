---
"@squide/firefly": major
"@squide/firefly-module-federation": major
"@squide/firefly-storybook": major
"@squide/react-router": major
---

Bumped the `react-router` peer dependency to `^8.2.0` (from `^7.15.0`). This is a breaking change — consuming applications must upgrade to React Router v8.

---
"@squide/firefly-webpack-configs": major
---

Bumped the `webpack-dev-server` peer dependency to `^6.0.0` (from `^5.2.4`). This is a breaking change — consuming applications must upgrade to webpack-dev-server v6.
